<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <style>
        body {
            background: linear-gradient(to right, #b3cef0, #4585f5);
            font-family: 'Ariel', sans-serif;
        }

        .container-header {
            display: flex;
            justify-content: space-between;
            margin: 2rem 5rem;
        }

        h1 {
            color: #fcfcfc;
            font-weight: bold;
        }

        .btn-add-blog {
            background-color: #3aa0fa;
            color: white;
            padding: 1rem 2rem;
            text-transform: uppercase;
            border: none;
            border-radius: 5px;
            transition: background 0.3s;
        }

        .btn-add-blog:hover {
            background-color: #0080ff;
        }

        .container-blogs {
            display: flex;
            flex-wrap: wrap;
            margin: 2rem;
        }

        .card {
            width: 18%;
            margin: 0.5rem;
        }

        .card-body {
            border: 1px solid #8fa6f8;
            border-radius: 5px;
            padding: 1rem;
            background-color: #d3e2ff;
            box-shadow: 0 4px 8px rgba(65, 73, 94, 0.899);
        }

        .card-title {
            color: #639afb;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .card-text {
            color: #fffdfd;
        }

        .btn-read {
            background-color: #508bef;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 0.5rem 1rem;
            text-transform: uppercase;
            transition: background 0.3s;
        }

        .btn-read:hover {
            background-color: #005bf7;
        }

        .btn-delete {
            background-color: #fa4747fe;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 0.5rem 1rem;
            text-transform: uppercase;
            transition: background 0.3s;
        }

        .btn-delete:hover {
            background-color: #f70000;
        }
    </style>
</head>
<body>
    <div class="container-header">
        <h1>All Blogs</h1>
        <a href="/add-blog" class="btn btn-add-blog">Add New Blog</a>
    </div>
    <div class="container-blogs">
        <% for(let item of allBlogs) { %>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><%= item.title  %></h5>
                    <p>By: <%= item.name %></p> 
                    <div class="d-flex justify-content-between">
                        <a href="/read-blog/<%= item._id %>" class="btn btn-read">Read</a>
                        <form action="/delete-blog/<%= item._id %>?_method=DELETE" method="POST">
                            <button type="submit" class="btn btn-delete">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>
